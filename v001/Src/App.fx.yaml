App As appinfo:
    BackEnabled: =false
    OnStart: =Set(pw_visible, false);
    StartScreen: =

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

